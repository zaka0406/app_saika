<div class="reservation">
    <div class="reservation-nav">
        <h1 class="reservation-title">管理者画面</h1>
        </div>

   <div class="calendar">
      <%= month_calendar events: @reservations do |date, reservations| %>
        <%= date.day %>
      <% end %>

 <div class="retrun">
    <%= link_to"戻る" , rails_admin_path %>
    </div>

      
</div>
      <%= form_with model: @reservation, url:update_admin_reservations_path ,local: true do |form| %>
	     <%= form.hidden_field :start_time, value: @start_time %>
      <%= form.submit '更新する' %>
        <% end %>

    
</div>